# `.samples`

Tracked example projects you can set up inside this workspace. Useful for getting started, and sometimes used for testing.

| Path | Role |
|------|------|
| `rails_sample_app/rails_sample_app_initialization.md` | Create a Compose-based Rails app under the workspace; browser access is super_projects default network localhost (`http://localhost:<published-port>`) |
